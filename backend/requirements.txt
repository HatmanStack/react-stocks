# ONNX Export Script Dependencies (scripts/export_onnx.py)
# NOT used in production - production uses requirements-ml.txt
#
# Install only when exporting model to ONNX format:
#   pip install -r requirements.txt
#   python scripts/export_onnx.py

transformers>=4.30.0
torch>=2.5.0
